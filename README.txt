Options
A. D3 javascript library
	1. Convert times to 30 min increments from an initial time point
	2. Graph each group in its own columns with Y top and bottom values equal to start and end points

B. Create table with classes matching timespan of 30 min increments
	1. Use jquery to insert rows with rowspan per event